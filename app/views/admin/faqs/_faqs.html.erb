<div class="sortable sortable--col-3" data-url="<%= sort_admin_faqs_path %>">
	<% faqs.each do |faq| %>
		<div class="sortable-item" id="<%= dom_id(faq)%>"><%= render 'admin/faqs/faq', faq: faq %></div>
	<% end %>
</div>